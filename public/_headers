# Cloudflare Pages 响应头配置
# HTML 永不缓存：保证浏览器每次都拿到最新的 <script> 引用。
# 否则旧 HTML + 新构建的文件名（内容哈希会变）会请求到已删除的资源，
# 而 Pages 对找不到的路径回退成 index.html（text/html），
# 浏览器拒绝把 HTML 当 JS 模块执行 -> 白页。
/
  Cache-Control: no-cache, no-store, must-revalidate

/index.html
  Cache-Control: no-cache, no-store, must-revalidate

# 带内容哈希的资源：内容一变文件名就变，可以长期强缓存
/assets/*
  Cache-Control: public, max-age=31536000, immutable
